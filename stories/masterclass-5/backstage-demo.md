# Backstage MCP Demo Script

## Scenario 1: Context-Aware Component Scaffolding

"I'm working on the `billing-service`. Can you fetch its context from the Backstage catalog and write the `catalog-info.yaml` for it?"

## Scenario 2: Standardizing API Definitions

"We need to add the `payment-gateway` API to Backstage. Please fetch the corporate schemas and generate the correct `API` kind YAML definition. Save it as `payment-gateway.yaml`."

## Scenario 3: Automating TechDocs Toil

"Generate the initial `index.md` for our TechDocs based on the corporate template. Please fill in a basic Mermaid diagram for the `billing-service` based on the catalog context you fetched earlier."
